
describe('Films', () => {
  test('Should Add a new film', () => {
  })
  test('Should list all registered films', () => {
  })
  test('Should insert new spectator', () => {
  })
  test('Should should list all spectators', () => {
  })
  test('Should mark a film as watched by a spectator', () => {
  })
  test('Should list a watched film list of a spectators', () => {
  })
  test('Should list how many spectators has watched a film', () => {
  })
})
